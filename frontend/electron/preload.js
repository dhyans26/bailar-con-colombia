// Runs in a privileged context with access to Node.js APIs, but exposes
// only what's explicitly bridged to the renderer via contextBridge.
// import { contextBridge } from 'electron'
// contextBridge.exposeInMainWorld('api', { ... })
